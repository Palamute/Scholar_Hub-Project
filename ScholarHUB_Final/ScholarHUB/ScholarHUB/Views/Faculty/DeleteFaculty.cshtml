@model ScholarHUB.Models.Faculty

@{
    ViewData["Title"] = "Delete Faculty";
    Layout = "~/Views/Shared/_Admin.cshtml";
}


<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h1>Admin - Delete Faculty</h1>

            <h3>Are You Sure You Want To Delete This?</h3>
            <div>
                <hr />
                <dl class="row">
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.FacultyId)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.FacultyId)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.FacultyName)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.FacultyName)
                    </dd>
                </dl>
                <form asp-action="DeleteFaculty" method="post">
                    <input type="hidden" asp-for="FacultyId" />
                    <button type="submit" onclick="return confirm('Are you sure you want to delete this?');" class="btn btn-danger">Confirm</button> |
                    <a asp-action="ListFaculty" class="btn btn-secondary">Cancel</a>
                    @Html.AntiForgeryToken()
                </form>
            </div>
        </div>
    </div>
</div>

